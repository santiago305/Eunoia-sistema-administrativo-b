# Adapters In

Puertos de entrada del modulo de autenticacion.

## Estructura

- `controllers/`: endpoints HTTP.
- `dtos/`: DTOs de entrada.
- `guards/`: guards basados en JWT.

## Notas

- El guard CSRF vive en `src/shared/utilidades/guards/csrf.guard.ts`.

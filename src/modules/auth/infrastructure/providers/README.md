# Providers

Implementaciones concretas de servicios de autenticacion.

## Archivos

- `argon2-password-hasher-read.repository.ts`: hash y verificacion con argon2.
- `jwt-token-read.repository.ts`: firma de access y refresh tokens con JWT.
- `strategy/`: estrategias Passport para access y refresh tokens.

# Entities (TypeORM)

Entidades ORM usadas para mapear tablas de base de datos.

## Archivos

- `user.entity.ts`: entidad TypeORM para la tabla `users`, con relacion a `Role` y campos como `email`, `password`, `deleted`, `avatarUrl` y `createdAt`.
